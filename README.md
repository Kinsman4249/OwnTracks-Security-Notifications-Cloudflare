# OwnTracks-Security-Notifications-Cloudflare
Cloudflare Worker that monitors HTTP Basic Auth login attempts to an OwnTracks Frontend instance and sends email alerts via SMTP2GO when new or notable IPs are seen.
